{
  "name": "genesis-lab",
  "version": "0.0.1",
  "description": "Genesis lab",
  "homepage": "https://github.com/mrHoft/genesis-lab#readme",
  "license": "ISC",
  "private": true,
  "tasks": {
    "client": "cd client && npm start",
    "client:build": "cd client && npm run build",
    "server": "cd server && deno task dev",
    "server:start": "cd server && deno task start",
    "lint": "deno lint client server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspace": [
    "client",
    "server"
  ]
}
